# BulkyBook Pre-Deploy Bug Register

Status values: `Fixed`, `Open`, `Accepted Risk`, `Needs Manual Verification`.

| ID | Priority | Status | Area | Defect / risk | Evidence | Fix / action | Related file / method |
|---|---|---|---|---|---|---|---|
| `EMAIL-01` | P0 | Fixed | Identity email | Runtime could remain in `LocalFile`, so Gmail never receives email. | Manual QA found local `.html` files while expecting Gmail. | Added SMTP secure socket support, sanitized failure messages, and Development diagnostics page. | `EmailSender.cs`, `DiagnosticsController.Email` |
| `EMAIL-02` | P1 | Fixed | Identity email | Register/external-login/manage-email flows could silently ignore failed SMTP delivery. | `IEmailSender.SendEmailAsync` returned no status to the caller. | Switched those pages to `IEmailDeliveryService.SendEmailWithResultAsync` and status messages. | `Register.cshtml.cs`, `ExternalLogin.cshtml.cs`, `Manage/Email.cshtml.cs` |
| `EMAIL-03` | P1 | Fixed | Identity security | `RegisterConfirmation` displayed a direct `ConfirmEmail` token link even when a real email sender exists. | Scaffold code had `DisplayConfirmAccountLink = true`. | Direct link now appears only in Development with `Email:Provider=LocalFile`. | `RegisterConfirmation.cshtml.cs` |
| `CART-01` | P0 | Fixed, needs manual verification | Cart | Badge could show stale value or row count instead of total quantity. | QA report. | Badge recalculates from persisted cart row quantity and updates session. | `ShoppingCartViewComponent.Invoke`, `HomeController.SyncCartSession` |
| `CART-02` | P0 | Fixed, needs manual verification | Cart | Cart updates/removals could leave stale session badge. | QA report. | Cart actions sync session and empty cart sets count to 0. | `CartController` |
| `CHECKOUT-01` | P0 | Fixed, needs manual verification | Checkout | Users could not reliably place orders when Stripe config/fallback varied. | QA report. | Server-side checkout branches now block missing Stripe safely or use Development fallback. | `CartController.SummaryPOST` |
| `CHECKOUT-02` | P0 | Fixed, needs manual verification | Checkout | Missing Stripe config could create bad order/payment state. | QA report. | Checkout blocks order creation when Stripe is missing and fallback disabled. | `CartController.SummaryPOST` |
| `CHECKOUT-03` | P0 | Fixed, needs manual verification | Checkout | Company delayed payment could leave `PaymentDueDate` default. | QA report. | Company checkout sets due date server-side. | `CartController.SummaryPOST` |
| `CHECKOUT-04` | P0 | Fixed, needs manual verification | Checkout | Required shipping fields/fallback profile values could be submitted. | QA report. | Placeholder values are stripped/validated and corrected profile values are saved. | `CartController.ValidateCheckoutProfile` |
| `CHECKOUT-05` | P0 | Fixed, needs manual verification | Checkout | Posted `OrderHeader.OrderTotal` could affect stored order total. | QA report. | Server recalculates total and removes model state for server-controlled fields. | `CartController.RemoveServerControlledCheckoutModelState` |
| `ORDER-02` | P0 | Fixed, needs manual verification | Orders | Customer `My Orders` pointed to Admin order index while index was staff-only. | QA report. | Order index is available to authenticated users; `GetAll` filters own orders for non-staff. | `OrderController.Index`, `OrderController.GetAll` |
| `PAYMENT-01` | P1 | Open | Stripe | Full Stripe webhook/idempotency is not implemented. | Code uses redirect confirmation and local fallback. | Keep as production risk unless a schema-free webhook pass is approved; durable idempotency usually needs schema/state design. | `CartController.OrderConfirmation`, `OrderController.PayNow` |
| `DATA-01` | P1 | Open | Data model | Money is stored as `double`/SQL `float`. | `OrderHeader.OrderTotal`, price fields. | Future schema migration to `decimal` required. Do not change in this pass. | `OrderHeader`, migrations |
| `STARTUP-01` | P1 | Accepted Risk | Startup/deployment | Startup seed hooks can create roles/admin if configured. | `Program.cs` calls `SeedDatabase()` on startup. | On Somee keep `Database__AutoMigrate=false`, `SeedData__EnableDemoData=false`, and production `SeedAdmin` blank unless intentionally creating admin. | `Program.cs`, `DbInitializer` |
| `WARN-01` | P2 | Open | Build hygiene | Identity scaffold nullable/analyzer warnings remain. | Release build passes with warnings. | Schedule warning cleanup after runtime fixes. | `Areas/Identity/**` |
| `UI-01` | P2 | Needs Manual Verification | UI/UX | Full responsive/browser UI pass not executed in this terminal-only run. | No browser server started because startup can touch DB. | Execute `BULKYBOOK_UI_UX_TEST_CASES.md` in target environment. | Razor views |
| `SOMEE-01` | P1 | Needs Manual Verification | Deployment | Somee SMTP/Stripe/DB behavior depends on host environment variables. | Not verifiable without deployed Somee app. | Execute Somee pre/post deployment checklists. | `docs/deployment/**` |
