@page
@model ForgotPasswordConfirmation
@{
    ViewData["Title"] = "Forgot password confirmation";
}

<h1>@ViewData["Title"]</h1>
<p>
    @(Model.StatusMessage ?? "If an account exists and can receive password reset email, reset instructions will be delivered by the configured email provider.")
</p>
